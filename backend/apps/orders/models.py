"""Order capture, line items and status transitions.

Models go here. Inherit from apps.common.models.BaseModel for a UUID primary
key plus created_at / updated_at.
"""

from apps.common.models import BaseModel  # noqa: F401
