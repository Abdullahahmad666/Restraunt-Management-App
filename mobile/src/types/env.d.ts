/**
 * Types for the virtual '@env' module created by react-native-dotenv.
 * Add an entry here for every key in .env.example.
 */
declare module '@env' {
  export const API_BASE_URL: string | undefined;
  export const API_TIMEOUT_MS: string | undefined;
}
