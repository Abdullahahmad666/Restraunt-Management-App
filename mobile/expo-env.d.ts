/// <reference types="expo/types" />

// Expo's ambient declarations. The Expo CLI regenerates this file and rewrites
// tsconfig.json#include, so nothing hand-written should live here - see
// src/types/env.d.ts for our own declarations.
